<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <ul class="nav navbar-nav">
      <li class="active">
          <%= link_to "", home_index_path, :class=>"glyphicon glyphicon-home" %>
      </li>
      <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Категории <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li>
              <% Category.all.each do |category| %>
                <%= link_to category.name, category_path(category) %>
              <% end %>    
            </li>
          </ul>
        </li>
              
    </ul>  
      <% if user_signed_in? %>
          <%= link_to 'Выйти', destroy_user_session_path, :method => :delete, :class=>"btn btn-info navbar-btn navbar-right"%>
        <%if current_user && current_user.admin? %>
          <%= link_to "Здравствуйте, AlexFlush", home_profile_path , :class=>"btn btn-default navbar-btn navbar-right"%>           
        <% else %>
          <%= link_to "Здравствуйте, #{current_user.email}", home_profile_path, :class=>"btn btn-default navbar-btn navbar-right"%>             
        <% end %>   
      <% else %>
        <%= link_to 'Зарегистрироваться', new_user_registration_path, :class=>"btn btn-default navbar-btn navbar-right"%>
        <%= link_to 'Войти', new_user_session_path, :class=>"btn btn-info navbar-btn navbar-right"%>
      <% end %>
     </div>      
  </div>
</nav>
     
